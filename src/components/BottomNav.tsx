import { Box, Stack } from "@mui/material";

const BottomNav = () => {
  return (
    <Box p={1} display={{ xs: "block", sm: "none" }} flex={1}>
      <Stack>BottomNav</Stack>
    </Box>
  );
};

export default BottomNav;
